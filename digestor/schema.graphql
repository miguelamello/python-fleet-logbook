type Reading {
  _id: ID!
  timestamp: String!
  metadata: Device!
}

type Device {
  source: String!
  latitude: String!
  longitude: String!
  utctime: String!
}

type Query {
  getReadings: [Reading]
}
